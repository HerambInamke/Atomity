import FeatureSection from "@/components/FeatureSection/FeatureSection";

export default function Home() {
  return (
    <main style={{ minHeight: "100vh", paddingTop: "clamp(40px, 8vw, 100px)" }}>
      <FeatureSection />
    </main>
  );
}
