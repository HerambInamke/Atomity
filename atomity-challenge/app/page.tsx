import FeatureSection from "@/components/FeatureSection/FeatureSection";

export default function Home() {
  return (
    <main style={{ minHeight: "100vh", padding: "clamp(32px, 6vw, 80px) clamp(12px, 3vw, 24px)" }}>
      <FeatureSection />
    </main>
  );
}
